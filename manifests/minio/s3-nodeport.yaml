apiVersion: v1
kind: Service
metadata:
  name: minio-s3-api
  namespace: minio-tenant
spec:
  type: NodePort
  selector:
    v1.min.io/tenant: personalization
  ports:
    - name: http-minio
      port: 9000
      targetPort: 9000
      nodePort: 30014
